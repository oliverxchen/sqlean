select field from table where a AND b

---

Tree(
    "query_expr",
    [
        Token("SELECT", "select"),
        Tree(
            "select_list",
            [Tree("select_item_unaliased", [Token("FIELD_NAME", "field")])],
        ),
        Token("FROM", "from"),
        Tree(
            "from_clause",
            [
                Tree("single_table_name", [Token("CNAME", "table")]),
                Tree(
                    "from_modifier",
                    [
                        Tree(
                            "where_clause",
                            [
                                Token("WHERE", "where"),
                                Tree(
                                    "binary_bool_operation",
                                    [
                                        Token("FIELD_NAME", "a"),
                                        Token("BINARY_BOOL_OPERATOR", "AND"),
                                        Token("FIELD_NAME", "b"),
                                    ],
                                ),
                            ],
                        )
                    ],
                ),
            ],
        ),
    ],
)
