select field from table where a

---

SELECT
    field,
FROM
    table
WHERE
    a

---

Tree(
    "query_file",
    [
        Tree(
            "query_expr",
            [
                Tree("select_statement", [Token("SELECT", "select")]),
                Tree("select_list", [Tree("select_item_unaliased", [Token("FIELD_NAME", "field")])]),
                Token("FROM", "from"),
                Tree(
                    "from_clause",
                    [
                        Tree("simple_table_name", [Token("CNAME", "table")]),
                        Token("WHERE", "where"),
                        Tree("bool_list", [Tree("first_bool_item", [Token("FIELD_NAME", "a")])]),
                    ],
                ),
            ],
        )
    ],
)
